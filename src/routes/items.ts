//import * as itemsController from '../controllers/items';
import { Router } from 'express';

const itemsRouter = Router();

//itemsRouter.post('/match', itemsController.matchItems);

export default itemsRouter;

